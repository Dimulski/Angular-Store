import { ToLowerHyphenPipe } from './to-lower-hyphen.pipe';

describe('ToLowerHyphenPipe', () => {
  it('create an instance', () => {
    const pipe = new ToLowerHyphenPipe();
    expect(pipe).toBeTruthy();
  });
});
